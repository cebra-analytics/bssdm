% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boyce.R
\name{boyce}
\alias{boyce}
\alias{boyce.default}
\alias{boyce.Rangebag}
\alias{boyce.Climatch}
\title{Calculate Boyce index for model validation}
\usage{
boyce(fit, ...)

\method{boyce}{default}(
  fit,
  obs,
  nclass = 0,
  window_width = "default",
  res = 100,
  pe_plot = FALSE,
  rm_duplicate = TRUE,
  method = "spearman",
  ...
)

\method{boyce}{Rangebag}(
  fit,
  x,
  obs,
  nclass = 0,
  window_width = "default",
  res = 100,
  pe_plot = TRUE,
  rm_duplicate = TRUE,
  method = "spearman",
  ...
)

\method{boyce}{Climatch}(
  fit,
  x,
  obs,
  nclass = 0,
  window_width = "default",
  res = 100,
  pe_plot = TRUE,
  rm_duplicate = TRUE,
  method = "spearman",
  ...
)
}
\arguments{
\item{fit}{A vector or \code{terra::SpatRaster} containing predicted
suitability values, or a fitted model object (\code{Rangebag} or
\code{Climatch}).}

\item{...}{Additional arguments passed to methods.}

\item{obs}{A vector containing predicted suitability values at validation
points (presence records), or a two-column matrix/data.frame of
xy-coordinates (if \code{fit} is a \code{SpatRaster}).}

\item{nclass}{Number of classes (integer) or vector with class thresholds.
If \code{nclass = 0} (default), the Boyce index is calculated with a
moving window (see \code{window_width} and \code{res} parameters).}

\item{window_width}{Width of the moving window. Default is \code{"default"},
which uses 1/10 of the suitability range.}

\item{res}{Resolution of the moving window (number of bins). Default is 100.}

\item{pe_plot}{Logical. If \code{TRUE} (default), plots the
predicted-to-expected ratio along the suitability classes.}

\item{rm_duplicate}{Logical. If \code{TRUE} (default), successive duplicated
values are excluded from the correlation calculation.}

\item{method}{Correlation method used to compute the Boyce index. Default is
\code{"spearman"}.}

\item{x}{Environmental/climate data with corresponding model variables as a
\code{terra::SpatRaster}, \verb{raster::Raster*}, \code{data.frame}, or
\code{matrix}. Required when \code{fit} is a model object.}
}
\value{
An object of class \code{boyce}, which is a list containing:
\itemize{
\item \code{cor}: Boyce index (correlation coefficient).
\item \code{f_ratio}: Predicted-to-expected ratio for each class interval.
\item \code{hs}: Mean habitat suitability values for each interval.
\item \code{indices}: Indices of values used in correlation (after removing
successive duplicates if \code{rm_duplicate = TRUE}).
}
}
\description{
Calculates the Boyce index as described in Hirzel et al. (2006) for
evaluating species distribution models using presence-only data.
}
\details{
The Boyce index measures how much model predictions differ from a random
distribution of observed presences across prediction classes. It is a
presence-only metric that does not require absence data. Values range from
-1 to 1, where positive values indicate model predictions consistent with
the distribution of presences, values close to zero indicate predictions no
different than random, and negative values indicate counter predictions.

This implementation is adapted from \code{ecospat.boyce} in the
\href{https://github.com/ecospat/ecospat}{ecospat package}, originally authored
by Blaise Petitpierre and Frank Breiner with updates by Flavien Collart
(GPL-3.0 licence).
}
\section{Methods (by class)}{
\itemize{
\item \code{boyce(default)}: Default method for vectors and SpatRaster

\item \code{boyce(Rangebag)}: Method for Rangebag model objects

\item \code{boyce(Climatch)}: Method for Climatch model objects

}}
\examples{
\dontrun{
# With a fitted Rangebag model
model <- rangebag(presence_data, env_data)
validation_coords <- data.frame(x = c(...), y = c(...))
boyce_result <- boyce(model, env_data, validation_coords)

# With prediction raster and presence values
predictions <- predict(model, env_data)
presence_values <- terra::extract(predictions, validation_coords)
boyce_result <- boyce(predictions, presence_values)
}
}
\references{
Hirzel, A. H., Le Lay, G., Helfer, V., Randin, C., & Guisan, A.
(2006). Evaluating the ability of habitat suitability models to predict
species presences. \emph{Ecological Modelling}, 199(2), 142-152.
\doi{10.1016/j.ecolmodel.2006.05.017}

Broennimann O, Di Cola V, Guisan A (2025). \emph{ecospat: Spatial
Ecology Miscellaneous Methods}. R package version 4.1.2,
\url{https://CRAN.R-project.org/package=ecospat}.
}
